import logging

from Stream import Stream

if __name__ == "__main__":
    logging.basicConfig(level=logging.INFO)

    stream = Stream()

    stream.append_node()
    stream.append_node()
    stream.append_node()



    sr.run(st, root)